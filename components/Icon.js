import React from 'react'
import { AntDesign } from '@expo/vector-icons'

export default ({ name, size, color, style }) => <AntDesign
    name={name}
    size={size}
    style={style}
    color={color}
/>